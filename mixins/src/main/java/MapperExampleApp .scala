


object MapperExampleApp extends App {

/*
  def query[ResultType, MappingType <: Table[ResultType, MappingType]]
    (mapping: Table[ResultType, MappingType]):Query[MappingType, ResultType] = {
                                          var q = new Query[MappingType,ResultType]
                                          return q
                                            }
*/  
//  val a = query(Perros).map("perros", _.nombre)
  
}
